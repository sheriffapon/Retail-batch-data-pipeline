name: batch_data_pipeline
version: '1.0.0'
config-version: 2

profile: batch_data_pipeline

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  batch_data_pipeline:
    staging:
      +schema: staging
      +materialized: view

    marts:
      +schema: marts
      +materialized: table